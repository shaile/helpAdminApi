name: Github actions

on:
  pull_request:
  push:
    branches:
      - main
      - development
  
jobs:
  build:   
   runs-on: ubuntu-latest
   steps: 
    - uses: actions@v1
    - name: Use Nodejs 14
      uses: actions@v1
      with:
        node-version: 14
    - name: npm install, build
      run: |
        npm run build --if--present
        npm run start
  
